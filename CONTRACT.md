# 对齐契约

判断标准只有一条: **跨人传递的东西必须一致, 人内部的东西一律自由。**

数据从一个人流到另一个人的地方就是接口, 接口上的名字必须逐字一致。
接口之外(你怎么读图、怎么起变量名、notebook 怎么组织)不要试图统一, 统一了只会互相掣肘。

---

## 一、硬契约 —— 必须逐字一致

拼错就直接报错或产出错误结果。这些名字不经群里通知不得修改。

| 名字 | 含义 | 谁产出 | 谁消费 | 不一致会怎样 |
|---|---|---|---|---|
| `manifest.csv` | 数据集唯一真相来源 | A | 全体 | — |
| `filepath` | 相对 `image_root` 的路径 | A | 全体 | 读不到图 |
| `class_id` | 整数类别, **连续 0..C-1** | A | 全体 | 见下方"最阴的坑" |
| `species_name` | 物种名字符串 | A | 全体 | 报告里类名对不上 |
| `split` | 取值只能是 `train` / `val` / `test`, 全小写 | A | 全体 | 划分为空, 静默跑出空集 |
| `class_names` | 按 `class_id` 升序的物种名列表 | `get_class_names()` | 全体 | 混淆矩阵行列错位 |
| `y_true` | `(N,)` int, 真实 `class_id` | 各方法 | `save_result` | — |
| `y_pred` | `(N,)` int, 预测 `class_id` | 各方法 | `save_result` | — |
| `y_score` | `(N, C)` float, 越大越可能。logits / `decision_function` / `predict_proba` 都行 | 各方法 | `save_result` | top-5 算错 |
| `train_s` | 训练耗时, 单位秒 | 各方法 | 主表 | 时间列没法横向比 |
| `test_s` | 测试耗时, 单位秒 | 各方法 | 主表 | 同上 |
| `results/<method>.npz` | 每个方法的唯一产出 | 各方法 | 汇总_D1 | 汇总脚本扫不到 |
| `save_result()` | 唯一的结果落盘入口 | `src/metrics.py` | 全体 | 指标口径分裂 |

### `class_id` 是最阴的坑

如果深度组用 `ImageFolder`(按文件夹名排序自动编号)、传统组用 `LabelEncoder`(按字符串排序),
两边的 class 0 可能是不同物种。**各自的准确率都正常, 但混淆矩阵一对比就全错**, 而且极难发现。

规则: `class_id` 只有一个来源 —— A 的 manifest。任何人不许自己编号。
`get_class_names()` 里已经 assert 了连续性, 别绕过它。

---

## 二、口径契约 —— 语义一致, 实现自由

名字不用一样, 但**含义必须一样**, 否则数字放进同一张表里没有意义。

| 概念 | 统一口径 | 常见的错误理解 |
|---|---|---|
| `train_s`(传统) | 建码本 + 特征提取 + 分类器 fit, **三段都算** | 只算 `clf.fit()` —— BoVW 的大头在特征提取, 只算 fit 会低估一个数量级 |
| `train_s`(深度) | 到收敛的总墙钟时间 | 算单 epoch, 或把 val 推理算进去 |
| `test_s` | 特征提取 + 推理的端到端时间 | 只算 `predict()`, 漏掉特征提取 |
| `val` 的用途 | 调参、选 checkpoint、早停 | 拿它当最终结果报告 |
| `test` 的用途 | **只在最终评估时读一次** | 边调参边看 test —— 这是 data leakage, 会全盘作废 |
| "收敛" | `curves.csv` 里 val loss 走平 | 跑完预设 epoch 就算收敛 |
| `seed` | 默认 0, 换 seed 必须在报告里写明 | 不记录, 结果不可复现 |

---

## 三、`method_name` 命名规范

这个字段会直接变成报告主表的行标签, 所以规范一点。全小写, 下划线分隔:

| 类型 | 格式 | 例子 |
|---|---|---|
| 单描述子 | `<描述子>_<分类器>` | `lbp_linear_svm`, `hog_random_forest` |
| BoVW | `bovw_k<码本大小>_<分类器>` | `bovw_k512_linear_svm` |
| 深度 | `<scratch\|pretrained>_<架构>` | `scratch_r18`, `pretrained_r50` |
| 深度消融 | `<基准>_<变量><值>` | `pretrained_r18_noaug`, `scratch_r18_sgd` |

深度组注意: `cfg.run_name` 会同时用作 `runs/<name>/` 和 `results/<name>.npz`,
所以 `run_name` 就是 `method_name`, 不要起两个名字。

---

## 四、明确不需要对齐的

写在这里是为了防止有人"为了统一而统一", 浪费时间还互相阻塞。

| 项 | 为什么放开 |
|---|---|
| 图像预处理 | 深度要 224×224 + ImageNet 归一化的 tensor; 传统可能要灰度、别的尺寸、SIFT 关键点。强行统一只会互相掣肘 |
| 读图代码 | `src/data.py` 依赖 torch, `src/traditional/io.py` 不依赖。这份重复是刻意的 —— 传统组不必装 torch |
| 内部函数名、变量名 | 只要不跨人传递就无所谓 |
| notebook 怎么组织 | notebook 是各自的运行入口, 共享逻辑才进 `.py` |
| 各自的中间缓存格式 | 特征矩阵存 `.npy` 还是 `.pkl` 随意, 只要最终落 `.npz` |

---

## 五、需要人对齐(不是代码对齐)的

代码管不住, 但比代码更容易出事:

| 事项 | 何时对齐 | 怎么做 |
|---|---|---|
| manifest 格式 | **A 开始处理数据之前** | 现在就发四列的表给 A |
| class list 冻结 | 数据交付当天 | 冻结后不许再改, 改了所有实验作废 |
| 基准超参 `BASELINE` | 深度组开跑之前 | 定死后所有消融相对它单变量变化 |
| 全组对齐会 | 第 5 天 | 确认所有人用的是同一份 manifest 和同一个 `save_result` |
| 谁跑哪些实验 | 每周 | 避免两个人跑了同一个配置, 或某个配置没人跑 |

---

## 改契约的流程

改硬契约 = 所有人重跑。所以:

1. 群里说明改什么、为什么
2. 确认谁需要重跑
3. 改完在群里发一句"契约已更新到 X"

`src/metrics.py` 和 `src/config.py` 的 `BASELINE` 属于此类。
